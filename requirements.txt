# Training / analysis stack — installed on the workstation (Apple M1, macOS).
# See scripts/setup_workstation.sh for the one macOS-specific build workaround.

# --- core RL ---
stable-baselines3==2.9.0
gymnasium==1.3.0
torch==2.13.0

# --- simulators ---
# panda-gym provides PandaReach-v3 (primary backend, matches the Part 1 plan).
panda-gym==3.0.7
pybullet==3.2.7
# gymnasium-robotics provides FetchReach-v4 (portable cross-simulator check).
gymnasium-robotics==1.4.2
mujoco==3.11.0

# --- numerics, logging, analysis ---
numpy==2.2.6
scipy==1.15.3
pandas==2.3.3
matplotlib==3.10.9
tensorboard==2.21.0

# --- demo capture and deliverables ---
imageio==2.37.4
imageio-ffmpeg==0.6.0
python-pptx==1.0.2
